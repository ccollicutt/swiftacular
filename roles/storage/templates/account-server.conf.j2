[DEFAULT]
devices = /srv/node
bind_ip = {{ ansible_enp0s10.ipv4.address }}
bind_port = 8080
workers = 2

[pipeline:main]
pipeline = account-server

[app:account-server]
use = egg:swift#account

[account-replicator]

[account-auditor]

[account-reaper]